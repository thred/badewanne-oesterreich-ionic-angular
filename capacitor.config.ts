import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Badewanne Österreich Ionic Angular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
